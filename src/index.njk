---
layout: base.njk
title: Home
description: Experience the Bauhaus movement through digital design - a museum-grade exhibition of geometric modernism, functional aesthetics, and revolutionary design philosophy
---

<!-- Hero Section -->
<section class="hero">
    <div class="container">
        <div class="hero-content">
            <!-- Geometric decorative elements -->
            <div class="geometric-decoration" aria-hidden="true">
                <div class="shape shape--circle"></div>
                <div class="shape shape--square"></div>
                <div class="shape shape--triangle"></div>
            </div>
            
            <h1 class="hero-title">
                Bauhaus Revival
            </h1>
            
            <p class="hero-subtitle">
                Form follows function. Where geometric purity meets digital innovation.
            </p>
            
            <div class="hero-cta">
                <a href="{{ '/projects/' | baseUrl }}" class="btn btn--primary">
                    Explore the Movement
                    <span aria-hidden="true">→</span>
                </a>
                <a href="{{ '/about/' | baseUrl }}" class="btn btn--outline">
                    Design Philosophy
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Featured Projects Section -->
<section class="featured-projects section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Core Principles</h2>
            <p class="section-subtitle">
                The foundational concepts that defined modernist design
            </p>
        </div>
        
        <div class="grid grid--3-col">
            {% set featuredProjects = collections.projects | filter('featured') | limit(3) %}
            {% if featuredProjects.length > 0 %}
                {% for project in featuredProjects %}
                <article class="card project-card card-geometric">
                    {% if project.data.image %}
                    <div class="card-image">
                        <img src="{{ project.data.image | baseUrl }}" 
                             alt="{{ project.data.title }}"
                             loading="lazy">
                    </div>
                    {% endif %}
                    
                    <div class="card-content">
                        <h3 class="card-title">
                            <a href="{{ project.url | baseUrl }}">
                                {{ project.data.title }}
                            </a>
                        </h3>
                        
                        <p class="card-description">
                            {{ project.data.description or (project.templateContent | excerpt) }}
                        </p>
                        
                        {% if project.data.tags %}
                        <div class="card-tags">
                            {% for tag in project.data.tags %}
                            {% if tag != "projects" %}
                            <span class="tag">{{ tag }}</span>
                            {% endif %}
                            {% endfor %}
                        </div>
                        {% endif %}
                        
                        {% if project.data.status %}
                        <span class="project-status status-{{ project.data.status }}">
                            {{ project.data.status }}
                        </span>
                        {% endif %}
                    </div>
                    
                    <div class="card-footer">
                        <a href="{{ project.url | baseUrl }}" class="card-link">
                            Explore Principle <span aria-hidden="true">→</span>
                        </a>
                    </div>
                </article>
                {% endfor %}
            {% else %}
                <!-- Placeholder content -->
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Form Follows Function</h3>
                        <p class="card-description">Design must serve purpose above all else. Every element justified by its utility.</p>
                        <div class="card-tags">
                            <span class="tag">1919-1933</span>
                            <span class="tag">Weimar</span>
                        </div>
                    </div>
                </article>
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Geometric Abstraction</h3>
                        <p class="card-description">Primary shapes as universal language. Circle, square, triangle define spatial harmony.</p>
                        <div class="card-tags">
                            <span class="tag">Modernism</span>
                            <span class="tag">Gropius</span>
                        </div>
                    </div>
                </article>
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Unity of Arts</h3>
                        <p class="card-description">Dissolving barriers between fine art, craft, and industrial design.</p>
                        <div class="card-tags">
                            <span class="tag">Dessau</span>
                            <span class="tag">Revolution</span>
                        </div>
                    </div>
                </article>
            {% endif %}
        </div>
        
        <div class="section-footer">
            <a href="{{ '/projects/' | baseUrl }}" class="btn btn--secondary">
                View All Principles
                <span aria-hidden="true">→</span>
            </a>
        </div>
    </div>
</section>

<!-- Recent Blog Posts Section -->
<section class="recent-posts section section--alt">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Historical Context</h2>
            <p class="section-subtitle">
                Understanding the movement that transformed modern design
            </p>
        </div>
        
        <div class="grid grid--3-col">
            {% set recentPosts = collections.blog | reverse | limit(3) %}
            {% if recentPosts.length > 0 %}
                {% for post in recentPosts %}
                <article class="card post-card">
                    <div class="card-content">
                        {% if post.data.date %}
                        <time class="card-date post-date" datetime="{{ post.data.date | dateFormat('YYYY-MM-DD') }}">
                            {{ post.data.date | dateFormat('MMMM D, YYYY') }}
                        </time>
                        {% endif %}
                        
                        <h3 class="card-title">
                            <a href="{{ post.url | baseUrl }}">
                                {{ post.data.title }}
                            </a>
                        </h3>
                        
                        <p class="card-description">
                            {{ post.data.description or (post.templateContent | excerpt) }}
                        </p>
                        
                        {% if post.data.tags %}
                        <div class="card-tags">
                            {% for tag in post.data.tags %}
                            {% if tag != "blog" %}
                            <span class="tag">{{ tag }}</span>
                            {% endif %}
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="card-footer">
                        <a href="{{ post.url | baseUrl }}" class="card-link">
                            Read More <span aria-hidden="true">→</span>
                        </a>
                        {% if post.templateContent %}
                        <span class="reading-time">{{ post.templateContent | readingTime }} min read</span>
                        {% endif %}
                    </div>
                </article>
                {% endfor %}
            {% else %}
                <!-- Placeholder exhibition content -->
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">1919-1933</time>
                        <h3 class="card-title">The Weimar Years</h3>
                        <p class="card-description">Founded by Walter Gropius in Weimar, Germany, the Bauhaus revolutionized art education by uniting fine arts and craft.</p>
                        <div class="card-tags">
                            <span class="tag">Origins</span>
                            <span class="tag">Gropius</span>
                        </div>
                    </div>
                </article>
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">1925-1932</time>
                        <h3 class="card-title">Dessau & Industrial Design</h3>
                        <p class="card-description">The move to Dessau marked the school's most productive period, producing iconic furniture and typography.</p>
                        <div class="card-tags">
                            <span class="tag">Moholy-Nagy</span>
                            <span class="tag">Albers</span>
                        </div>
                    </div>
                </article>
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">1933-Present</time>
                        <h3 class="card-title">Legacy & Influence</h3>
                        <p class="card-description">Though closed by the Nazi regime, Bauhaus principles spread globally, shaping modernism for generations.</p>
                        <div class="card-tags">
                            <span class="tag">International Style</span>
                            <span class="tag">Modernism</span>
                        </div>
                    </div>
                </article>
            {% endif %}
        </div>
        
        <div class="section-footer">
            <a href="{{ '/blog/' | baseUrl }}" class="btn btn--secondary">
                View All Articles
                <span aria-hidden="true">→</span>
            </a>
        </div>
    </div>
</section>

<!-- Call to Action Section -->
<section class="cta-section section">
    <div class="container">
        <div class="cta-content">
            <h2 class="cta-title">Experience Bauhaus Today</h2>
            <p class="cta-text">
                Discover how these revolutionary principles continue to shape contemporary design and digital experiences.
            </p>
            
            <div class="cta-actions">
                <a href="{{ '/contact/' | baseUrl }}" class="btn btn--accent btn--lg">
                    Explore the Exhibition
                    <span aria-hidden="true">→</span>
                </a>
            </div>
        </div>
    </div>
</section>
